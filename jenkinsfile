pipeline {
    agent any

    stages {
        stage('Checkout source code') {
            steps{
                sh "echo running "
        }
   }
        stage('Build') {
            steps {
                // nodejs(nodeJSInstallationName: 'Node') {
                //     bat 'npm install -g node-gyp'
                //     bat 'npm i'
                //     bat 'npm run start'
                // }
                sh 'npm i'

            }
            
        }
        // stage('Test') {
        //     steps {
        //         bat 'npm run start'
        //     } 
        // }        

    }
}
